import React from "react";

const DataCard = props => {
  return (
    <div>
        <h2>{props.title}</h2>
      {/* <img alt="outer space" src={props.config.transformResponse.url} /> */}
    </div>
  );
};


export default DataCard;
